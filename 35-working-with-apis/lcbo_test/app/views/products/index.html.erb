<h1>Products</h1>
<% @response["result"].each do |product| %>
  <h2><%= product["name"] %></h2>
  <% if product["image_thumb_url"] %>
    <%= image_tag(product["image_thumb_url"]) %>
  <% end %>
<% end %>
